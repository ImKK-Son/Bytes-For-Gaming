/* =============================================================
   Bytes For Gaming — Site settings (plain English)
   ============================================================= */
window.BFG_CONFIG = {

  // PRODUCT PHOTOS
  // Leave this ON. To show a real photo for a product, save the photo into
  // the  images/products/  folder named after the product, e.g.
  //   images/products/razer-deathadder-v3.jpg
  // (the full list of names is in images/products/README.md).
  // No photo yet? The product just shows its clean built-in icon instead.
  autoProductImages: true,

  // ADVANCED / OPTIONAL — most people can ignore this.
  // If you set up affiliate feeds (see README "Affiliate feeds"), put the
  // generated file path here to show real live prices. Otherwise leave blank
  // and the site uses friendly price estimates.
  offersFile: ''
};
